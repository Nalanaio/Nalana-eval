[pytest]
testpaths = tests
addopts = -v --import-mode=importlib
filterwarnings =
    ignore::pytest.PytestCollectionWarning
markers =
    blender_worker: requires a blender binary on PATH (skipped in unit-test containers)
